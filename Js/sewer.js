/*class sewer {
    
    constructor() {
        this.img = new Image()
        this.img.src = "./images/alcantarilla-removebg-preview.png"
        this.x = canvas.width;
        let randomSewer = Math.random() * (canvas.height - 90)
        let randomSewerFinal = Math.floor(randomSewer)
        this.y = randomSewerFinal;
        this.w = 80;
        this.h = 60;
        this.speed = 5;
    }

    drawSewer = () => {
        ctx.drawImage(this.img, this.x, this.y, this.w, this.h)
    }

    moveSewer = () => {
        this.x = this.x - this.speed
    }
}
*/